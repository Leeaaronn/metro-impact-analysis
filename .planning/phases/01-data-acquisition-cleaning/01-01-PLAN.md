---
phase: 01-data-acquisition-cleaning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - Makefile
  - src/__init__.py
  - src/acquire.py
  - data/raw/.gitkeep
  - data/clean/.gitkeep
  - tests/__init__.py
  - notebooks/.gitkeep
autonomous: true
requirements:
  - DATA-SOURCES-A
  - DATA-SOURCES-D
  - REPRODUCIBILITY

must_haves:
  truths:
    - "Project scaffold exists with correct directory layout from CLAUDE.md"
    - "requirements.txt contains all allowed libraries with pinned versions"
    - "Makefile has setup, test, notebook, and all targets"
    - "src/acquire.py has working download functions for ridership and station data"
    - "Raw ridership CSV exists in data/raw/ with 2009-2025 monthly data"
    - "Raw station locations CSV exists in data/raw/ with all Metro rail stations"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned dependency list"
      contains: "pandas"
    - path: "Makefile"
      provides: "Build automation"
      contains: "make test"
    - path: "src/acquire.py"
      provides: "Data download functions"
      exports: ["download_ridership_csv", "create_station_locations"]
    - path: "data/raw/ridership_monthly.csv"
      provides: "Raw ridership data"
    - path: "data/raw/station_locations.csv"
      provides: "Station coordinates"
  key_links:
    - from: "Makefile"
      to: "requirements.txt"
      via: "pip install -r"
      pattern: "pip install.*requirements"
    - from: "src/acquire.py"
      to: "data/raw/"
      via: "file write"
      pattern: "data/raw/"
---

<objective>
Set up the project scaffold and acquire the two primary datasets: Metro ridership (line-level monthly from Streets For All) and station locations (manually verified CSV).

Purpose: Establish the working environment and download the core data needed for all subsequent analysis. The ridership data is the foundation of the entire DiD analysis, and station locations are needed for any spatial operations.

Output: Working project structure, requirements.txt, Makefile, ridership CSV in data/raw/, station locations CSV in data/raw/.
</objective>

<execution_context>
@C:\Users\leeaa\metro-impact-analysis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\leeaa\metro-impact-analysis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-data-acquisition-cleaning/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project scaffold with requirements.txt and Makefile</name>
  <files>requirements.txt, Makefile, src/__init__.py, src/acquire.py, tests/__init__.py, data/raw/.gitkeep, data/clean/.gitkeep, notebooks/.gitkeep</files>
  <read_first>
    - CLAUDE.md (directory layout and allowed libraries)
  </read_first>
  <action>
Create the directory structure exactly as specified in CLAUDE.md:
- `data/raw/` and `data/clean/` directories (with .gitkeep)
- `src/` directory with empty `__init__.py`
- `tests/` directory with empty `__init__.py`
- `notebooks/` directory (with .gitkeep)

Create `requirements.txt` with these exact pinned versions:
```
pandas==2.2.3
numpy==1.26.4
scipy==1.14.1
statsmodels==0.14.4
seaborn==0.13.2
matplotlib==3.9.3
jupyter==1.1.1
pytest==8.3.4
requests==2.32.3
nbconvert==7.16.6
```

Create `Makefile` with these targets:
```makefile
.PHONY: setup test notebook all clean

setup:
	python -m pip install -r requirements.txt

test:
	python -m pytest tests/ -v

notebook:
	jupyter nbconvert --to notebook --execute notebooks/analysis.ipynb --output analysis_executed.ipynb

all: setup test notebook

clean:
	rm -rf data/clean/*.csv
	rm -f notebooks/analysis_executed.ipynb
```

Create `src/__init__.py` as empty file.
Create `tests/__init__.py` as empty file.

Do NOT create src/clean.py, src/stats.py, or src/viz.py yet -- those belong to later phases.
  </action>
  <verify>
    <automated>python -c "import pathlib; dirs = ['data/raw', 'data/clean', 'src', 'tests', 'notebooks']; missing = [d for d in dirs if not pathlib.Path(d).is_dir()]; assert not missing, f'Missing dirs: {missing}'" && python -c "f=open('requirements.txt'); lines=f.read(); f.close(); assert 'pandas==' in lines and 'pytest==' in lines, 'Missing deps'"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains exactly 10 libraries: pandas, numpy, scipy, statsmodels, seaborn, matplotlib, jupyter, pytest, requests, nbconvert
    - Each library has a pinned version (== notation)
    - Makefile contains targets: setup, test, notebook, all, clean
    - Makefile setup target contains "pip install -r requirements.txt"
    - Directories exist: data/raw/, data/clean/, src/, tests/, notebooks/
    - src/__init__.py exists (can be empty)
    - tests/__init__.py exists (can be empty)
  </acceptance_criteria>
  <done>Project scaffold matches CLAUDE.md layout, all directories exist, requirements.txt has pinned versions of all allowed libraries, Makefile has all required targets.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/acquire.py with download functions and acquire raw data</name>
  <files>src/acquire.py, data/raw/ridership_monthly.csv, data/raw/station_locations.csv</files>
  <read_first>
    - CLAUDE.md (coding standards, allowed libraries)
    - .planning/phases/01-data-acquisition-cleaning/01-RESEARCH.md (Section 1a for ridership schema, Section 4b for station coordinates)
    - .planning/REQUIREMENTS.md (Data Sources section A and D)
  </read_first>
  <action>
Create `src/acquire.py` with two public functions, both with full type hints per CLAUDE.md:

**Function 1: `download_ridership_csv(output_path: str = "data/raw/ridership_monthly.csv") -> pathlib.Path`**

Download the ridership CSV from Streets For All. The site at ridership.streetsforall.org has a CSV download feature. Use `requests` to GET the data.

Implementation approach:
1. Try downloading from `https://ridership.streetsforall.org` — inspect the site to find the CSV export URL. The research indicates a "Download" button that exports filtered CSV.
2. If the direct download URL is not trivially discoverable, use the NTD fallback: `https://data.streetsforall.org/` or the Cal-ITP NTD page at `https://analysis.dds.dot.ca.gov/ntd_monthly_ridership/`.
3. If automated download fails (404, auth required, etc.), follow CLAUDE.md stop condition: log to STATE.md and raise an informative error.

The downloaded CSV should contain columns matching the schema from research: Year, Month, Line Name, Day Group, Avg. Daily Boardings, Line Category.

Filter to: Line Category == "Train" only (exclude bus), Day Group == "Weekday" (primary analysis metric).

Save to `data/raw/ridership_monthly.csv`.

**Function 2: `create_station_locations(output_path: str = "data/raw/station_locations.csv") -> pathlib.Path`**

Create a manually-verified CSV of ALL Metro rail stations with these columns:
- `station_name`: str
- `line`: str (current letter designation: A, B, C, D, E, K)
- `lat`: float
- `lon`: float
- `opened_date`: str (YYYY-MM-DD)
- `is_foothill_2016`: bool (True for the 6 Phase 2A stations)

Include at minimum the 6 Foothill Extension stations with these coordinates (from research, to be verified via GTFS or Google Maps):
- Arcadia: 34.1417, -118.0287
- Monrovia: 34.1356, -118.0019
- Duarte/City of Hope: 34.1311, -117.9712
- Irwindale: 34.1283, -117.9344
- Azusa Downtown: 34.1360, -117.9068
- APU/Citrus College: 34.1363, -117.8920

Also include representative control stations from other lines (at least 3-5 per line for B/D, C, E lines) with their coordinates. These will be used for treatment/control grouping.

Write the CSV to `data/raw/station_locations.csv`.

**Main block**: Add `if __name__ == "__main__":` that calls both functions and prints summary (row counts, date ranges for ridership).

After creating acquire.py, RUN it: `python src/acquire.py` to actually download/create the data files.

IMPORTANT: If the Streets For All download fails (404, requires browser interaction, etc.), implement a fallback that:
1. Checks if data/raw/ridership_monthly.csv already exists (user may manually place it)
2. If not, prints clear instructions for manual download and the exact URL
3. Updates STATE.md with a blocker note

Use `requests` library (not httpx) per CLAUDE.md allowed libraries.
  </action>
  <verify>
    <automated>python -c "import pandas as pd; df=pd.read_csv('data/raw/ridership_monthly.csv'); assert len(df) > 100, f'Only {len(df)} rows'; assert 'Line Name' in df.columns or 'line_name' in df.columns, f'Cols: {list(df.columns)}'; print(f'Ridership: {len(df)} rows')" && python -c "import pandas as pd; df=pd.read_csv('data/raw/station_locations.csv'); assert len(df) >= 6, f'Only {len(df)} stations'; assert df[df.get('is_foothill_2016', df.get('is_foothill_2016', pd.Series([False]))).astype(bool)].shape[0] == 6, 'Missing Foothill stations'; print(f'Stations: {len(df)} rows')"</automated>
  </verify>
  <acceptance_criteria>
    - src/acquire.py contains `def download_ridership_csv(` with type hints
    - src/acquire.py contains `def create_station_locations(` with type hints
    - src/acquire.py imports only from allowed libraries (requests, pandas, pathlib, etc.)
    - data/raw/ridership_monthly.csv exists with >100 rows of monthly ridership data
    - data/raw/ridership_monthly.csv contains columns for year/date, line name, and boardings
    - data/raw/station_locations.csv exists with >=6 rows
    - data/raw/station_locations.csv contains columns: station_name, line, lat, lon, opened_date, is_foothill_2016
    - Exactly 6 rows have is_foothill_2016 == True
    - Variable names in src/acquire.py use snake_case
  </acceptance_criteria>
  <done>Both raw data files exist in data/raw/ with expected schemas and row counts. src/acquire.py has documented, type-hinted download functions that can reproduce the data acquisition.</done>
</task>

</tasks>

<verification>
- All directories from CLAUDE.md layout exist
- requirements.txt has all allowed libraries pinned
- Makefile targets: setup, test, notebook, all, clean
- data/raw/ridership_monthly.csv has monthly ridership data spanning 2009-2025
- data/raw/station_locations.csv has all Foothill Extension stations plus control stations
- `python -c "import src.acquire"` succeeds without error
</verification>

<success_criteria>
- Project scaffold complete and matching CLAUDE.md spec
- Raw ridership data downloaded with >100 monthly observations
- Station locations CSV created with 6 Foothill stations marked
- All files committed, no import errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-acquisition-cleaning/01-01-SUMMARY.md`
</output>
